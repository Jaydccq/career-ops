# Tech Debt Tracker

Durable record of known gaps that should not live only in chat.

| Date | Area | Gap | Impact | Suggested Fix | Status |
|------|------|-----|--------|---------------|--------|
| 2026-04-21 | `batch/batch-runner.sh` | Report-number discovery loops over `reports/*.md` and assumes every basename begins with a numeric prefix. Non-report markdown files such as `reports/CLAUDE.md` can make arithmetic expansion fail. | Batch workers can start with an empty or corrupt report number, producing duplicate or malformed reports and pending tracker-addition TSVs. | Restrict report-number parsing to filenames matching `^[0-9]+-.*\\.md$`, add a shell test fixture with `reports/CLAUDE.md`, and fail before spawning workers if no report number can be reserved. | Open |
| 2026-04-22 | Bridge Vitest timeout config | The batch-runner e2e tests and `src/server.test.ts` background-adapter failure test can exceed Vitest's default 5s timeout during `npm run verify`; the same failed tests passed with `--testTimeout=20000`. | Full pipeline verification can report a false red even when bridge behavior passes under a realistic timeout. | Set explicit per-test/file-level timeouts or a bridge test config timeout for the known slow tests, then keep `npm run verify` on the default command. | Open |
| 2026-04-23 | Scanner browser lifecycle | Newgrad and LinkedIn scan flows rely on different browser runtimes (`newgrad-scan` uses Playwright persistent context; `linkedin-scan` uses bb-browser). Cleanup behavior is only partially surfaced in operator output, and stale browser state can still be confused with active scan tabs. | Operators can end scans with cluttered browser state, stale login assumptions, or uncertainty about whether LinkedIn/JobRight sessions are available. | Add shared scanner preflight/postflight diagnostics that list the browser runtime in use, verify login state for the selected source, close only tabs opened by the current run, and report orphaned browser processes. | Partially fixed for `newgrad-scan`: default headless bundled Chromium, bridge preflight before browser launch, scan browser closes after detail enrichment, and an old orphaned Playwright browser was cleaned up. |

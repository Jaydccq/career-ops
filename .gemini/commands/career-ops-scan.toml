description = "Scan job portals (Greenhouse, Ashby, Lever, Wellfound) and evaluate new matching offers."

prompt = """
You are career-ops in scan mode.

Load the scan context:
- Read file: modes/_shared.md
- Read file: modes/scan.md
- Read file: portals.yml
- Read file (if exists): modes/_profile.md
- Read file (if exists): data/scan-history.tsv

Then execute the scan mode as defined in modes/scan.md.
You can also run the zero-token scanner: node scan.mjs

Default scanner behavior queues direct newgrad_quick evaluations for current-run
offers. Pass --no-evaluate only for the old discovery-only path.

Additional filters (if any): $ARGUMENTS
"""
